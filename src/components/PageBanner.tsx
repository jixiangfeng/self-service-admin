import type { ReactNode } from 'react';

interface PageBannerProps {
  title: string;
  subtitle?: string;
  icon?: ReactNode;
}

const PageBanner: React.FC<PageBannerProps> = () => null;

export default PageBanner;
